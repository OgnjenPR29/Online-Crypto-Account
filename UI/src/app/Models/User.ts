export interface User {
    Email: string | null,
    First_Name: string | null,
    Last_Name: string | null,
    Address: string | null,
    City: string | null,
    Country: string | null,
    Phone: string | null,
    Password: string | null
}